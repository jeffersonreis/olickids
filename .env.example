# Keystatic Cloud project slug — only needed in production (Netlify).
# Format: your-team/olic-kids  (from keystatic.cloud dashboard)
# Prefixed with VITE_ so Vite exposes it to the browser (admin UI).
VITE_KEYSTATIC_CLOUD_PROJECT=your-team/olic-kids
